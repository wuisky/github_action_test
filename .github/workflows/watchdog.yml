name: reviewdog
on:
  pull_request:
  push:
    tags:
      - 'v*'
jobs:
  check_tag:
    name: check_tag
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2

      - name: Get tag
        id: tag
        uses: devops-actions/action-get-tag@v1.0.3
        with:
          strip_v: false # Optional: Remove 'v' character from version
          default: latest # Optional: Default version when tag not found

      - name: Use tag
        run: echo ${{steps.tag.outputs.tag}}

      # - uses: olegtarasov/get-tag@v2.1.3
      #   id: tagName

      # - name: "run"
      #   run: |
      #     echo ${{ steps.tagName.outputs.tag }}

      - name: SSH Debug
        if: ${{ always() }}
        uses: mxschmitt/action-tmate@v3